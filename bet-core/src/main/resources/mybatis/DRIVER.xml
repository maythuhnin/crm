<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.eniac.projects.bet.mappers.IDriverMapper">

	<resultMap 			type="DriverBean" 			id="DriverResult">
		<id 			property="id" 				column="id" />
		<result 		property="name" 			column="name" />
		<result		 	property="phone" 			column="phone" />
		<result		 	property="loanAmount" 		column="loan_amount" />
		<result 		property="status" 			column="status" />
		<result 		property="updatedId" 		column="updated_id" />
		<result 		property="updatedDateTime" 	column="updated_datetime" />
	</resultMap>

	<insert id="insertDriver" parameterType="DriverBean" useGeneratedKeys="true" keyProperty="id">
		INSERT INTO driver(
			name,
			<if test="phone != null">
				phone,
			</if>
			<if test="loanAmount != null">
				loan_amount,
			</if>
			status,
			updated_id,
			updated_datetime
			)
		VALUES(
			#{name},
			<if test="phone != null">
				#{phone},
			</if>
			<if test="loanAmount != null">
				#{loanAmount},
			</if>
			1,
			#{updatedId},
			NOW()
		)
	</insert>
	
	<update id="updateDriver" parameterType="DriverBean">
		UPDATE DRIVER SET 
			<if test="name != null">
				NAME = #{name}
			</if>
			<if test="phone != null">
				,PHONE = #{phone}
			</if>
			<if test="loanAmount != null">
				,LOAN_AMOUNT = #{loanAmount}
			</if>
			<if test="status != null">
				,STATUS = #{status}
			</if>
		WHERE ID =#{id} 
	</update>
	
	<update id="deleteDriver" parameterType="int">
		UPDATE DRIVER SET STATUS = 0
		WHERE ID =#{id}
	</update>
	
	<select id="selectForDatatable" resultType="map">
		SELECT 
		 id,
		 name,
		 loan_amount AS loanAmount,
		 phone
		
		FROM driver
		WHERE status != 0
	</select>
	
	<select id="selectForDropDown" resultMap="DriverResult">
		SELECT 
		 id,
		 name,
		 phone
		
		FROM driver
		WHERE status != 0
	</select>
	
	<select id="selectById" parameterType="int" resultMap="DriverResult">
		SELECT * FROM driver WHERE id = #{id} AND status != 0
	</select>
	
</mapper>