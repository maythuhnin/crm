<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.eniac.projects.bet.mappers.IDestinationMapper">

	<resultMap 			type="DestinationBean" 			id="DestinationResult">
		<id 			property="id" 				column="id" />
		<result 		property="name" 			column="name" />
		<result		 	property="isOrder" 			column="is_order" />
		<result		 	property="status" 			column="status" />
		<result 		property="updatedId" 		column="updated_id" />
		<result 		property="updatedDateTime" 	column="updated_datetime" />
	</resultMap>

	<insert id="insertDestination" parameterType="DestinationBean" useGeneratedKeys="true" keyProperty="id">
		INSERT INTO destination(
			name,
			is_order,
			status,
			updated_id,
			updated_datetime
			)
		VALUES(
			#{name},
			#{isOrder},
			1,
			#{updatedId},
			NOW()
		)
	</insert>
	
	<update id="updateDestination" parameterType="DestinationBean">
		UPDATE DESTINATION SET 
			<if test="name != null">
				NAME = #{name}
			</if>
			<if test="isOrder != null">
				,IS_ORDER = #{isOrder}
			</if>
		WHERE ID =#{id} 
	</update>
	
	<update id="deleteDestination" parameterType="int">
		UPDATE DESTINATION SET STATUS = 0
		WHERE ID =#{id}
	</update>
	
	<select id="selectForDatatable" resultType="map">
		SELECT 
		d.id,
		d.name,
		d.is_order AS isOrder
		
		FROM destination d
		WHERE d.status != 0
	</select>
	
	<select id="selectForDropDown" resultMap="DestinationResult">
		SELECT 
		 id,
		 name,
		 is_order AS isOrder
		
		FROM destination
		WHERE status != 0
	</select>
	
</mapper>